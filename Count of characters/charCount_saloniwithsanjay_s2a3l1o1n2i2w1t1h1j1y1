const str = 'aabcccccaad'

const strArray = str.split('')
const charCount = {}

//{element: charCount[element]}
strArray.forEach(element =>{
  if(charCount[element] === undefined){
    charCount[element] = 1
  }else{
    charCount[element]++
  }
})

console.log(charCount)
